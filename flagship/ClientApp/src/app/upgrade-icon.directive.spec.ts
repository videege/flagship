import { UpgradeIconDirective } from './upgrade-icon.directive';

describe('UpgradeIconDirective', () => {
  it('should create an instance', () => {
    const directive = new UpgradeIconDirective();
    expect(directive).toBeTruthy();
  });
});
